[pytest]

markers =
    smoke: smoke test
    integrity: test to check integrity
    uniqueness: test to check uniqueness
    unmarked: marker for tests without category


addopts =
    -v
    -s
    --tb=short
    --html=report.html --self-contained-html


testpaths = tests